import { AuthRoutes } from './auth.navigator.enums';
import {
  AuthRoutesParamList,
  AuthRoutesNavigationProps,
  AuthRouteProps,
} from './auth.navigator.types';

export { AuthRoutes };
export type { AuthRoutesParamList, AuthRoutesNavigationProps, AuthRouteProps };
